{
  "name": "drix-recruit",
  "version": "1.0.0",
  "description": "DRiX Recruit — score a reseller/partner for a vendor's partner program (focused on one product), then generate defended recruitment strategies + an outreach kit (opening email, 5-email drip, phone scripts).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check auth.js && node --check db.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "drix-brain": "github:stevewinfieldtx/DRiX-Brain#v0.7.0",
    "express": "^4.21.2",
    "mammoth": "^1.8.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "pg": "^8.20.0",
    "unzipper": "^0.12.3"
  }
}
